# Local MySQL/phpMyAdmin credentials
# Override by environment variables if needed:
#   AIPA_DB_URL, AIPA_DB_USERNAME, AIPA_DB_PASSWORD
spring.datasource.username=${AIPA_DB_USERNAME:root}
spring.datasource.password=${AIPA_DB_PASSWORD:}
